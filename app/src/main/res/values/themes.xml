<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- DayNight so the window / configuration follows AppCompatDelegate (Compose theme + delegate stay aligned). -->
    <style name="Theme.MiddinInnovatie" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
